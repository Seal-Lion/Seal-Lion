name: Daily README Update

on:
  schedule:
    - cron: '0 0 * * *'  # 매일 자정 UTC (한국시간 오전 9시)
  workflow_dispatch:  # 수동 실행 버튼

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Update README
      run: |
        DATE=$(date +%Y-%m-%d)
        PERCENT=$((RANDOM%100+1))
        echo "## 🚀 Daily Progress (${PERCENT}%)" >> README.md
        echo "**${DATE} KST**" >> README.md
        echo "✅ Unity C#: Kitten's Dreamland 개발" >> README.md
        echo "✅ Notion 게임기획 문서 작성" >> README.md
        echo "✅ 정보처리기사 필기 공부" >> README.md
        echo "" >> README.md
        echo "---" >> README.md
        echo "*🦁 Seal-Lion Auto Update*" >> README.md
        
    - name: Commit changes
      uses: stefanzweifel/git-auto-commit-action@v5
      with:
        commit_message: "📝 Daily Update"
