name: Daily Log Update

on:
  schedule:
    - cron: '0 0 * * *'  # 매일 자정 UTC (한국시간 오전 9시)
  workflow_dispatch:  # 수동 실행 버튼

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Update daily log
      run: |
        DATE=$(date +%Y-%m-%d)
        PERCENT=$((RANDOM%100+1))
        echo "## 🚀 Daily Progress (${PERCENT}%)" >> daily-log.md
        echo "**${DATE} KST**" >> daily-log.md
        echo "✅ Unity C#: Kitten's Dreamland 개발" >> daily-log.md
        echo "✅ Notion 게임기획 문서 작성 및 수정" >> daily-log.md
        echo "✅ 정보처리기사 준비" >> daily-log.md
        echo "" >> daily-log.md
        echo "---" >> daily-log.md
        echo "*🦁 Seal-Lion Auto Update*" >> daily-log.md
        
    - name: Commit changes
      uses: stefanzweifel/git-auto-commit-action@v5
      with:
        commit_message: "📝 Daily Update"

